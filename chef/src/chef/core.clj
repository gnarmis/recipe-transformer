(ns chef.core)

; Other Namespaces:
; chef.yummly has some preliminary API consuming stuff, but Yummly doesn't work right now
; chef.config is a git-ignored namespace with all sorts of app keys
; chef.food has code relating to the manipulation of food data

;(defn -main
;  "I don't do a whole lot."
;  [& args]
;  (println "Hello, World!"))
